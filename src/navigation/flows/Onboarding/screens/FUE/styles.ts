import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  bottomWrapper: { gap: 20, paddingBottom: 32, paddingHorizontal: 20 },
  paddedWrapper: {
    paddingHorizontal: 20,
  },
  pagination: { alignSelf: "center", flexDirection: "row", gap: 12 },
  scrollWrapper: { gap: 20 },
  wrapper: { flex: 1, gap: 40, paddingTop: 40 },
});

export default styles;
