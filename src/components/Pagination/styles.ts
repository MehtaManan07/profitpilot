import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    aspectRatio: 1,
    borderRadius: 50,
    overflow: "hidden",
    transform: [
      {
        rotateZ: "0deg",
      },
    ],
  },
});

export default styles;
